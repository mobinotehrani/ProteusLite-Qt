# بخش هشتم: کنترل شبیه‌سازی و حل صحیح مسیرهای سری

[بازگشت به README اصلی](README.md)

بخش هشتم چرخه اجرای مدار را از حالت خودکار و مبهم خارج می‌کند. کاربر اکنون می‌تواند مدار را اجرا کند، در همان لحظه متوقف نگه دارد، وضعیت موقت را بازنشانی کند یا شبیه‌سازی را یک گام جلو ببرد. در این بازنویسی، فقط رابط کنترل اصلاح نشده است؛ منطق انتشار ولتاژ در مسیرهای شامل مقاومت و LED نیز دوباره طراحی شده تا ظاهر قطعه با وضعیت واقعی مدار هماهنگ باشد.

شاخه این مرحله:

```text
feature/simulation-control
```

## چرا موتور قبلی نتیجه نادرست می‌داد

موتور قبلی ولتاژ خروجی منابع، Clock و گیت‌ها را روی شبکه پخش می‌کرد، اما یک قطعه دوپایه غیرفعال مانند مقاومت را جزئی از معادله شبکه در نظر نمی‌گرفت. در مدار زیر، ولتاژ گره اول مشخص بود، ولی گره بین مقاومت و LED هیچ‌وقت حل نمی‌شد:

```text
Clock OUT → Resistor → LED → GND
```

به همین دلیل سیم خروجی Clock قرمز و آبی می‌شد، اما LED روی `OFF` و جریان روی صفر باقی می‌ماند. این رفتار از مدار نبود؛ از محدود بودن حل‌کننده شبکه بود.

## حل‌کننده جدید شبکه

`Section06Controller` اکنون محاسبه را در سه لایه انجام می‌دهد:

1. خروجی قطعات فعال و اتصال‌های مستقیم مانند Switch بسته جمع‌آوری می‌شوند.
2. گره‌های مجهول در شبکه مقاومتی با روش گره‌ای حل می‌شوند.
3. جریان شاخه به مدل مقاومت و LED برگردانده می‌شود تا متن runtime، هشدارها و رسم قطعه به‌روز شوند.

مقاومت با قانون اهم مدل می‌شود. LED از یک مدل خطی تکه‌ای استفاده می‌کند: تا قبل از رسیدن ولتاژ مستقیم به `Vf` خاموش است و پس از آن با مقاومت دینامیکی آموزشی ده اهم جریان می‌گیرد. این مدل برای پروژه آموزشی قابل توضیح و قابل تکرار است و برخلاف نسخه قبلی، مسیر سری را واقعاً حل می‌کند.

برای نمونه، در مدار پنج ولت با LED دارای `Vf=1.8 V` و مقاومت یک کیلو اهم، جریان تقریبی شاخه چنین است:

```text
I = (5 - 1.8) / (1000 + 10) ≈ 3.17 mA
```

در این حالت LED روشن می‌شود، سیم قبل از مقاومت HIGH است، گره نزدیک LED یک مقدار آنالوگ دارد و سیم متصل به GND در وضعیت LOW قرار می‌گیرد.

## رفتار Run، Pause، Stop و Step

پنل `Simulation Console` چهار فرمان دارد:

- `Run` اجرای پیوسته را آغاز می‌کند یا پس از Pause ادامه می‌دهد.
- `Pause` زمان و خروجی فعلی را ثابت نگه می‌دارد.
- `Stop` زمان و شمارنده را صفر می‌کند و حالت runtime قطعات را به نقطه آغاز برمی‌گرداند.
- `Step` یک گام کنترل‌شده جلو می‌رود.

میان‌برها:

| فرمان | میان‌بر |
|---|---|
| Run | `F5` |
| Pause | `F6` |
| Stop | `Shift+F5` |
| Step | `F10` |

اگر Clock در مدار باشد، طول Step از نیم‌دوره سریع‌ترین Clock به دست می‌آید. در همان Step هر MCU حداکثر یک دستور اجرا می‌کند.

## ظاهر پنل‌ها

عنوان‌های مرحله‌ای از رابط نهایی حذف شده‌اند:

- مانیتور عمومی با عنوان `Circuit Monitor` نمایش داده می‌شود.
- قطعات ADC، DAC، MCU، حافظه، LCD و Keypad در `Advanced Devices` دیده می‌شوند.
- کنترل اجرا در `Simulation Console` قرار دارد.

مانیتورهای سمت راست به صورت Tab کنار یکدیگر قرار می‌گیرند. دکمه‌های Run، Pause، Stop و Step به ترتیب سبز، نارنجی، قرمز و آبی هستند و حالت غیرفعال آن‌ها نیز خوانا باقی می‌ماند.

## رنگ سیم‌ها

رنگ سیم هنگام Run یا Pause از ولتاژ حل‌شده همان گره به دست می‌آید:

- قرمز: HIGH
- آبی: LOW
- نارنجی: مقدار آنالوگ
- خاکستری خط‌چین: گره شناور یا تعریف‌نشده
- ارغوانی: درایورهای متناقض

Tooltip سیم علاوه بر نوع سیگنال، ولتاژ عددی را نیز نمایش می‌دهد. در حالت Stop همه سیم‌ها به رنگ عادی طراحی برمی‌گردند.

## تعامل زنده

هنگام اجرا، ورودی‌های تعاملی همچنان قابل استفاده‌اند:

- Switch با کلیک باز و بسته می‌شود.
- Push Button با نگه داشتن ماوس فعال می‌ماند.
- Keypad اتصال سطر و ستون کلید فشرده‌شده را برقرار می‌کند.
- Potentiometer با Wheel تغییر می‌کند.

تغییر ورودی باعث محاسبه دوباره شبکه می‌شود و نتیجه بدون Restart روی قطعات و سیم‌ها دیده می‌شود.

## هشدارهای قابل مشاهده

موتور موارد زیر را به‌صورت صریح گزارش می‌کند:

- نبودن GND
- متصل نبودن پایه برگشت منبع به GND
- وجود دو درایور با ولتاژ متناقض روی یک گره
- شناور یا تکین بودن شبکه غیرفعال
- بیشتر شدن جریان LED از مقدار مجاز تنظیم‌شده
- ورودی‌های تعریف‌نشده قطعات دیجیتال

## محدوده حل‌کننده

حل گره‌ای این بازنویسی برای منابع، Clock، مقاومت، LED و اتصال‌های مستقیم طراحی شده و مسیرهای سری و تقسیم مقاومتی را به‌درستی محاسبه می‌کند. مدل Capacitor و Inductor همچنان مدل آموزشی بخش ششم را استفاده می‌کند و جای تحلیل گذرای SPICE را نمی‌گیرد. این محدودیت در رابط پنهان نشده و README آن را صریح بیان می‌کند.

## تست اصلی مقاومت و LED

1. Clock، مقاومت یک کیلو اهم، LED و دو GND قرار دهید.
2. پایه `RET` کلاک را به GND وصل کنید.
3. مسیر `OUT → Resistor → LED → GND` را بسازید.
4. فرکانس Clock را روی `1 Hz` بگذارید.
5. Run را بزنید.
6. در نیم‌دوره HIGH، LED باید روشن و جریان آن غیرصفر باشد.
7. در نیم‌دوره LOW، LED باید خاموش شود.
8. سیم منبع در HIGH قرمز و در LOW آبی می‌شود.
9. گره بین مقاومت و LED هنگام روشن بودن معمولاً آنالوگ و نارنجی است.
10. با مقاومت صد اهم باید هشدار عبور جریان از حد تنظیم‌شده LED دیده شود.

## تست‌های بازگشتی

پس از تست مدار اصلی، این موارد نیز باید بررسی شوند:

- منبع DC، Battery و Clock
- مقاومت تکی و تقسیم ولتاژ دو مقاومتی
- Switch و Push Button در حال Run
- گیت‌های منطقی و D Flip-Flop
- ADC و DAC با ورودی حل‌شده شبکه
- Step برای Clock و MCU
- Stop و صفر شدن زمان و شمارنده
- حذف و اضافه کردن قطعه در حالت Stop
- Rotate، Mirror، Properties، سیم‌کشی و Junction

## فایل‌های جایگزین این بازنویسی

```text
section06controller.h
section06controller.cpp
wireitem.h
wireitem.cpp
section07controller.h
section07controller.cpp
section08controller.h
section08controller.cpp
CMakeLists.txt
README.md
SECTION06_README_FA.md
SECTION07_README_FA.md
SECTION08_README_FA.md
```

`SHA256SUMS.txt` در این بسته قرار نگرفته است، چون برای Build لازم نیست و باید فقط پس از نهایی شدن تمام فایل‌ها دوباره تولید شود.

## جمع‌بندی

بخش هشتم اکنون فقط رنگ سیم یا چند دکمه نیست. زمان شبیه‌سازی، حل شبکه، جریان شاخه، وضعیت LED، مانیتورها و ظاهر سیم از یک نتیجه مشترک استفاده می‌کنند. بنابراین حالتی که سیم HIGH باشد ولی LED بدون دلیل خاموش بماند، در مسیرهای پشتیبانی‌شده رخ نمی‌دهد.
