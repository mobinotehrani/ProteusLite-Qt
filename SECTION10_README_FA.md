# بخش دهم و نهایی - مدیریت پروژه و کنترل کیفیت مدار

این بخش آخر پروژه است و روی همان ساختاری که تا پایان بخش نهم وجود داشت اضافه شده است. هدف این مرحله این نبود که معماری برنامه دوباره نوشته شود؛ قابلیت‌های نهایی مستقیماً به `Section05Controller`، `Section06Controller` و `MainWindow` فعلی متصل شده‌اند تا رفتار بخش‌های قبلی دست‌نخورده بماند.

## قابلیت‌های اضافه‌شده

### منوهای قابل مشاهده و میان‌برها

برای اینکه قابلیت‌های نهایی فقط به Shortcut وابسته نباشند، Menu Bar برنامه به‌صورت صریح داخل پنجره اصلی ساخته و نمایش داده می‌شود. منوهای `File`، `Edit`، `View` و `Tools` همیشه بالای Toolbar قرار می‌گیرند. بنابراین Save/Save As/Export، Undo/Redo و Design Check هم از رابط گرافیکی و هم از میان‌برهای صفحه‌کلید قابل دسترسی هستند.

رنگ متن و حالت انتخاب آیتم‌های منو نیز به‌صورت صریح در Style Sheet تعریف شده است تا روی تم روشن یا تیره ویندوز، گزینه‌های منو همیشه خوانا بمانند. ترتیب منوها `File`, `Edit`, `View`, `Tools` است.

### ذخیره واقعی پروژه

گزینه‌های زیر به منوی File اضافه شده‌اند:

- `Save Project` با میان‌بر `Ctrl+S`
- `Save Project As...` با میان‌بر `Ctrl+Shift+S`

فایل پروژه با فرمت JSON ذخیره می‌شود. برخلاف Open اولیه پروژه که فقط Metadata را می‌خواند، فایل جدید اطلاعات واقعی مدار را نیز نگه می‌دارد:

- نام و ابعاد پروژه
- نوع، موقعیت و Reference هر قطعه
- Rotation و Mirror قطعات
- State و Propertyهای قطعات (داده‌های باینری مثل حافظه MCU به صورت Base64 امن ذخیره می‌شوند)
- Junctionها
- ابتدا و انتهای Wireها و مسیر آن‌ها
- یک Marker از وضعیت شبیه‌سازی در زمان Save

ذخیره‌سازی با `QSaveFile` انجام می‌شود تا اگر نوشتن فایل نیمه‌کاره ماند، فایل قبلی خراب نشود.

### Open و بازسازی واقعی مدار

وقتی یک پروژه JSON از مسیر قبلی `Open Project...` باز می‌شود، `MainWindow` ابتدا Metadata آن را می‌خواند و سپس Section 10 مدار ذخیره‌شده را روی بوم بازسازی می‌کند.

برای جلوگیری از وابستگی فایل ذخیره‌شده به IDهای داخلی، هنگام Load شناسه‌های Component و Junction دوباره تولید می‌شوند و Endpointهای Wire با یک Map داخلی به شناسه‌های جدید وصل می‌شوند. در نتیجه فایل به شمارنده‌های یک اجرای خاص برنامه وابسته نیست.

فایل‌های XML و JSONهای قدیمی همچنان برای Metadata قابل باز شدن هستند، اما بازسازی کامل مدار برای فایل‌هایی انجام می‌شود که با فرمت نسخه نهایی Section 10 ذخیره شده باشند. این تصمیم باعث می‌شود نمونه‌فایل‌های قدیمی پروژه به اشتباه با ساختار جدید تفسیر نشوند.

### Undo و Redo

منوی `Edit` شامل این دو فرمان است:

- `Undo` با `Ctrl+Z`
- `Redo` با `Ctrl+Y` یا `Ctrl+Shift+Z`

History در حافظه نگه داشته می‌شود. هر وضعیت مدار به شکل Snapshot JSON ثبت می‌شود و حداکثر 60 وضعیت آخر نگهداری می‌شود. تغییرات پشت سر هم هنگام Drag با یک Delay کوتاه یکی می‌شوند تا حرکت یک قطعه ده‌ها Undo جدا نسازد.

Undo/Redo شامل تغییرات اصلی مدار است، از جمله:

- اضافه و حذف قطعه
- جابه‌جایی
- Rotate و Mirror
- تغییر Properties
- سیم‌کشی
- Junction

با انجام یک تغییر جدید بعد از Undo، شاخه Redo قبلی حذف می‌شود؛ مشابه رفتار ویرایشگرهای معمولی.

### خروجی PNG

گزینه `Export Circuit as PNG...` با میان‌بر `Ctrl+Shift+E` به File اضافه شده است.

هنگام Export انتخاب آبی دور قطعات موقتاً برداشته می‌شود، کل Scene مدار روی تصویر سفید Render می‌شود و بعد Selection قبلی برمی‌گردد. برای جلوگیری از مصرف حافظه بیش از حد، تصویرهای بسیار بزرگ با حفظ نسبت ابعاد تا سقف مناسب Scale می‌شوند.

### Design Rule Check و Log

یک پنل پایین صفحه با نام `Simulation Log / DRC` اضافه شده است. این پنل پیام‌های Save، Load، Undo/Redo، Export، تغییر وضعیت شبیه‌سازی و Warningهای تولیدشده توسط خود قطعات را ثبت می‌کند. پیش از Run موفق نیز پیام صریح `DRC OK` ثبت می‌شود تا وضعیت بدون خطا هم در ترمینال مشخص باشد.

فرمان `Tools > Run Design Check` با میان‌بر `Ctrl+Shift+D` موارد زیر را بررسی می‌کند:

- Inputهای رهاشده و بدون اتصال
- نبودن Ground به عنوان Warning
- اتصال کوتاه دو ترمینال یک Voltage/Battery/Clock روی یک Node
- Conflict بین Driverها در زمان شبیه‌سازی

پیش از ورود موتور به حالت `RUNNING` یک Run Guard اجرا می‌شود. اگر خطای Blocking مثل مدار خالی، Floating Input، اتصال خروجی منبع به GND، اتصال دو سر یک منبع به یک Node یا منبع‌های ولتاژ ناسازگار روی یک Node وجود داشته باشد، Run اصلاً شروع نمی‌شود و علت در Log ثبت می‌شود. Conflictهایی که فقط هنگام حل شبکه قابل مشاهده‌اند نیز در زمان اجرا تشخیص داده می‌شوند و Simulation را متوقف می‌کنند.

## تغییر کوچک در ComponentItem

برای اینکه Save/Load قابل اعتماد باشد، `ComponentItem` چند API کوچک گرفته است:

- خواندن Rotation و وضعیت Mirror
- برگرداندن Visual State
- تنظیم Reference ذخیره‌شده
- Refresh کردن Pinها پس از Restore شدن State

Reference Counter نیز هنگام Load با Referenceهای عدددار هماهنگ می‌شود تا بعد از باز کردن یک پروژه، قطعه جدید Reference تکراری نگیرد.

## تغییر Section05Controller

مدل و آیتم‌های مدار همچنان در Section 5 مالکیت می‌شوند. Section 10 به جای دستکاری مستقیم Hashهای خصوصی، از دو API جدید استفاده می‌کند:

```cpp
QJsonObject circuitSnapshot() const;
bool restoreCircuitSnapshot(const QJsonObject &snapshot, QString &errorMessage);
```

همچنین Signal زیر اضافه شده است:

```cpp
void circuitChanged();
```

این Signal پایه History است و باعث می‌شود Undo/Redo به ساختار داخلی Section 5 وابسته نشود.

## فایل‌های این مرحله

فایل‌های جدید:

```text
section10controller.h
section10controller.cpp
SECTION10_README_FA.md
```

فایل‌های تغییرکرده:

```text
CMakeLists.txt
mainwindow.h
mainwindow.cpp
section05controller.h
section05controller.cpp
section06controller.h
section06controller.cpp
componentitem.h
componentitem.cpp
```

نسخه پروژه در `CMakeLists.txt` از `0.9` به `1.0` تغییر کرده است.

## تست پیشنهادی قبل از Push نهایی

### تست Save / Open

1. یک پروژه جدید بسازید.
2. Ground، Voltage، Resistor و LED قرار دهید و سیم‌کشی کنید.
3. یکی از قطعات را Rotate کنید و مقدار Resistor را تغییر دهید.
4. `Ctrl+S` را بزنید و فایل JSON را ذخیره کنید.
5. برنامه را ببندید و دوباره باز کنید.
6. فایل را از `Open Project...` انتخاب کنید.
7. موقعیت قطعات، Wireها، مقدارها و Transformها را مقایسه کنید.

### تست Undo / Redo

1. یک قطعه اضافه کنید.
2. آن را جابه‌جا و Rotate کنید.
3. یک Wire ایجاد کنید.
4. چند بار `Ctrl+Z` بزنید و مراحل را به عقب برگردانید.
5. با `Ctrl+Y` مراحل را دوباره جلو ببرید.
6. بعد از یک Undo، یک تغییر جدید ایجاد کنید و بررسی کنید Redo قبلی دیگر فعال نباشد.

### تست Export

1. چند قطعه و Wire روی بوم داشته باشید.
2. یک قطعه را Select کنید.
3. `Ctrl+Shift+E` را بزنید.
4. PNG ذخیره‌شده را باز کنید.
5. مدار باید دیده شود و Highlight انتخاب در تصویر نباشد.

### تست DRC

1. یک گیت منطقی با Input رهاشده قرار دهید و Design Check را اجرا کنید.
2. پیام Floating Input باید در Log دیده شود.
3. Input را سیم‌کشی کنید و دوباره Check بزنید.
4. منفی منبع Voltage را به Ground وصل کنید و سپس مثبت همان منبع را هم به همان Node برگردانید؛ DRC باید Short Circuit را ثبت کند.
5. یک مدار صحیح بسازید و بررسی کنید پیام Blocking Error وجود نداشته باشد.

### تست بازگشتی

بعد از اضافه شدن بخش نهایی، موارد زیر نیز یک بار بررسی شوند:

- New/Open اولیه
- Grid، Snap، Zoom و Pan
- Place، Delete، Rotate و Mirror
- Properties
- Wire و Junction
- قطعات بخش 6 و 7
- Run، Pause، Stop و Step
- رنگ زنده Wireها
- Voltage Probe، Voltmeter، Ammeter و Oscilloscope

## جمع‌بندی

در نسخه 1.0، مدار دیگر فقط داخل حافظه برنامه زندگی نمی‌کند. کاربر می‌تواند پروژه را ذخیره و دوباره باز کند، تغییرات خود را Undo/Redo کند، خروجی تصویری بگیرد و قبل یا حین اجرا خطاهای رایج طراحی را در یک Log مشخص ببیند. این قابلیت‌ها به ساختار موجود اضافه شده‌اند و منطق مدل مدار همچنان در Section 5 و منطق شبیه‌سازی در Section 6 باقی مانده است.
