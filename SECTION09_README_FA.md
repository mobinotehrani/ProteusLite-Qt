# بخش نهم - ابزارهای اندازه‌گیری

این مرحله ابزارهای اندازه‌گیری را به موتور شبیه‌سازی بخش‌های قبلی وصل می‌کند. هدف این است که مقدارهایی که کاربر روی بوم می‌بیند از همان وضعیت شبکه‌ای بیایند که برای LED، سیم‌ها، ADC و بقیه قطعات استفاده می‌شود؛ بنابراین برای نمایش ولتاژ یا جریان یک محاسبه جدا و ناسازگار ساخته نشده است.

## قابلیت‌های این بخش

سه قسمت اصلی پیاده‌سازی شده‌اند:

- پروب ولتاژ برای خواندن سریع ولتاژ سیم، Junction یا پایه با نگه داشتن نشانگر ماوس روی آن
- ولت‌متر و آمپرمتر دیجیتال به‌عنوان قطعات واقعی کتابخانه که روی بوم قرار می‌گیرند و سیم‌کشی می‌شوند
- اسیلوسکوپ دوکاناله با ورودی‌های `CH1`، `CH2` و `GND`، نمودار زنده و تنظیم `Time/Div` و `Volts/Div`

ابزارهای این بخش در حالت `Run` به‌صورت زنده به‌روز می‌شوند. در `Pause` مقدار فعلی حفظ می‌شود. `Step` یک نمونه جدید مطابق همان گام شبیه‌سازی ایجاد می‌کند و `Stop` نمایشگرهای عددی را ریست می‌کند و پنجره اسیلوسکوپ را می‌بندد.

## پروب ولتاژ

در پنل `Measurements` دکمه `Voltage Probe` قرار دارد. پس از فعال کردن آن و در حالی که شبیه‌سازی در حالت Run یا Pause است، می‌توان نشانگر ماوس را روی یکی از این موارد برد:

- سیم
- Junction
- پایه یک قطعه

کنترلر نزدیک‌ترین endpoint واقعی مدار را پیدا می‌کند و مقدار همان گره را از `Section06Controller` می‌خواند. مقدار کنار نشانگر و داخل پنل اندازه‌گیری نمایش داده می‌شود.

حالت‌های قابل نمایش:

```text
5.000 V
0.000 V
FLOAT / UNDEFINED
CONFLICT
```

در حالت Stop پروب عمداً اندازه‌گیری انجام نمی‌دهد، چون مقدارهای شبکه در این حالت برای ویرایش مدار ریست شده‌اند.

قطعه `Voltage Probe` که از قبل در گروه `Instruments` کتابخانه وجود داشت نیز فعال شده است. اگر آن را روی بوم قرار دهید و پایه `V` را به یک گره وصل کنید، مقدار همان گره روی خود قطعه نمایش داده می‌شود.

## ولت‌متر دیجیتال

ولت‌متر دو پایه دارد:

```text
V+
V-
```

مقدار نمایش داده شده برابر اختلاف ولتاژ دو پایه است:

```text
V = V+ - V-
```

ولت‌متر فقط از وضعیت حل‌شده شبکه استفاده می‌کند و به مدار بار اضافه نمی‌کند. ورودی‌های آن مانند ورودی اندازه‌گیری با امپدانس بالا در نظر گرفته شده‌اند.

رفتارهای مهم:

- اگر هر دو پایه مقدار معتبر داشته باشند، اختلاف آن‌ها با سه رقم اعشار نمایش داده می‌شود.
- اگر یکی از پایه‌ها شناور یا نامشخص باشد، `FLOAT` نمایش داده می‌شود.
- اگر مدار هیچ GND نداشته باشد، `ERR` نمایش داده می‌شود.
- در Pause آخرین مقدار معتبر حفظ می‌شود.
- در Stop نمایشگر به `0.000 V` برمی‌گردد.

## آمپرمتر دیجیتال

آمپرمتر دو پایه سری دارد:

```text
I+
I-
```

برای اینکه قرار دادن آمپرمتر مسیر مدار را قطع نکند، در حل‌کننده شبکه به‌عنوان یک شاخه با مقاومت داخلی بسیار کوچک `1 mΩ` مدل شده است. این مقدار در مقایسه با مقاومت‌های عادی مدار ناچیز است، ولی باعث می‌شود شاخه از نظر عددی تعریف‌شده باقی بماند و جریان آن مستقیماً از حل شبکه به دست بیاید.

نمایش جریان با توجه به اندازه مقدار به صورت خودکار بین واحدهای زیر تغییر می‌کند:

```text
uA
mA
A
```

علامت منفی به معنی عبور جریان در جهت مخالف `I+ → I-` است.

اگر آمپرمتر در مسیری قرار بگیرد که عملاً بار ندارد، مانند ورودی ایده‌آل یک گیت منطقی، جریان نزدیک صفر خواهد بود. در Stop مقدار نمایشگر `0.000 A` می‌شود.

## اسیلوسکوپ دوکاناله

قطعه اسیلوسکوپ سه پایه دارد:

```text
CH1
CH2
GND
```

هر دو کانال نسبت به پایه `GND` خود اسیلوسکوپ اندازه‌گیری می‌شوند. این کار باعث می‌شود اتصال اسیلوسکوپ در شماتیک مشخص و قابل جابه‌جایی باشد و هر کانال واقعاً به یک گره مدار وصل شود.

پنجره `Oscilloscope` شامل این موارد است:

- نمودار دوکاناله زنده
- کانال اول با Trace سبز
- کانال دوم با Trace آبی روشن
- کنترل `Time scale` بر حسب `ms/div`
- کنترل مستقل `CH1 scale` و `CH2 scale` بر حسب `V/div`
- نمایش عددی آخرین مقدار هر کانال
- نمایش نام اسیلوسکوپ فعال و وضعیت Run/Pause

شبکه نمودار ۱۰ تقسیم افقی و ۸ تقسیم عمودی دارد. تا زمانی که زمان شبیه‌سازی از عرض زمانی انتخاب‌شده کمتر باشد، موج از سمت چپ نمودار به سمت راست رشد می‌کند. بعد از پر شدن بازه زمانی، پنجره به صورت Rolling ادامه پیدا می‌کند و نمونه‌های قدیمی‌تر از بازه دید خارج می‌شوند.

اگر چند اسیلوسکوپ روی بوم قرار داده شود، فعلاً اسیلوسکوپی که Reference کوچک‌تری دارد به‌عنوان منبع پنل انتخاب می‌شود. این تصمیم باعث می‌شود رفتار پنل قطعی و قابل پیش‌بینی بماند.

## ارتباط با موتور شبیه‌سازی

برای بخش نهم دو API خواندنی به `Section06Controller` اضافه شده است:

```cpp
WireSignal signalForComponentPin(const QString &componentId, int pinIndex) const;
std::optional<double> branchCurrentForComponent(const QString &componentId) const;
```

اولی ولتاژ حل‌شده یک پایه را بدون تکرار منطق گراف برمی‌گرداند. دومی جریان شاخه قطعاتی را که در حل شبکه شرکت دارند در اختیار ابزار اندازه‌گیری قرار می‌دهد.

این دو متد فقط اطلاعات نتیجه شبیه‌سازی را می‌خوانند و زمان یا وضعیت مدار را تغییر نمی‌دهند.

## فایل‌های این بخش

```text
section09controller.h
section09controller.cpp
oscilloscopewidget.h
oscilloscopewidget.cpp
```

برای اتصال ابزارها به حل‌کننده فعلی این فایل‌ها نیز تغییر کرده‌اند:

```text
section06controller.h
section06controller.cpp
CMakeLists.txt
README.md
SECTION06_README_FA.md
```

قطعات `VoltageProbe`، `Voltmeter`، `Ammeter` و `Oscilloscope` از قبل در کتابخانه پروژه و در گروه `Instruments` تعریف شده بودند. در این مرحله تعریف تکراری برای آن‌ها ساخته نشده و همان مدل‌ها به منطق اندازه‌گیری متصل شده‌اند.

## تست ۱ - پروب ولتاژ

1. یک منبع DC پنج ولت و GND روی بوم قرار دهید.
2. خروجی منبع را به یک سیم متصل کنید و برگشت منبع را به GND ببندید.
3. Run را بزنید.
4. `Voltage Probe` پنل Measurements را فعال کنید.
5. نشانگر را روی سیم 5V ببرید؛ باید `5.000 V` دیده شود.
6. نشانگر را روی سیم GND ببرید؛ باید `0.000 V` دیده شود.
7. یک گره بدون درایور ایجاد کنید؛ پروب باید `FLOAT / UNDEFINED` نشان دهد.

## تست ۲ - ولت‌متر

مدار زیر را بسازید:

```text
5V Source +  ───── V+
GND          ───── V-
```

در Run باید مقدار زیر روی ولت‌متر دیده شود:

```text
5.000 V
```

سپس GND را از کل مدار حذف کنید. در این حالت ولت‌متر باید `ERR` نشان دهد.

## تست ۳ - آمپرمتر

برای بررسی سری بودن آمپرمتر مدار زیر مناسب است:

```text
5V Source → Ammeter → 1kΩ Resistor → Red LED → GND
Source RET → GND
```

با LED قرمز دارای ولتاژ آستانه حدود `1.8 V`، جریان باید در محدوده چند میلی‌آمپر باشد. با مدل فعلی LED و مقاومت یک کیلو اهم مقدار حدود `3.17 mA` انتظار می‌رود. مقدار آمپرمتر، مقاومت و LED باید با یکدیگر سازگار باشند.

برای بررسی ورودی با امپدانس بالا نیز آمپرمتر را در مسیر یک ورودی گیت قرار دهید. در نبود بار واقعی، مقدار باید نزدیک صفر باشد.

## تست ۴ - اسیلوسکوپ

1. یک Clock با فرکانس `1 Hz` قرار دهید و پایه برگشت آن را به GND وصل کنید.
2. `CH1` اسیلوسکوپ را به خروجی Clock وصل کنید.
3. `CH2` را به یک منبع DC ثابت، مثلاً `2.5 V`، متصل کنید.
4. پایه `GND` اسیلوسکوپ را به GND مدار وصل کنید.
5. Run را بزنید.
6. در CH1 باید موج مربعی بین صفر و پنج ولت دیده شود.
7. در CH2 باید خط تقریباً ثابت روی 2.5 ولت دیده شود.
8. `Time scale` را تغییر دهید و فشردگی افقی موج را بررسی کنید.
9. `CH1 scale` و `CH2 scale` را جداگانه تغییر دهید و ارتفاع هر Trace را بررسی کنید.
10. Pause را بزنید؛ نمودار نباید با گذشت زمان واقعی جلو برود.
11. در Pause یک Step بزنید؛ فقط گام جدید شبیه‌سازی باید به نمودار اضافه شود.
12. Stop را بزنید؛ نمونه‌ها باید پاک شوند و پنجره اسیلوسکوپ بسته شود.

## تست بازگشتی بخش‌های قبلی

بعد از اضافه شدن ابزارهای اندازه‌گیری این موارد نیز دوباره بررسی شوند:

- Clock، مقاومت و LED بخش هشتم
- Run، Pause، Stop و Step
- رنگ سیم‌های HIGH، LOW، Analog و Floating
- Switch و Push Button هنگام Run
- ADC و DAC
- MCU و Step دستوربه‌دستور
- جابه‌جایی، Rotate و Mirror قطعات
- سیم‌کشی و Junction

آمپرمتر تنها قطعه جدیدی است که عمداً در حل الکتریکی به‌عنوان شاخه سری شرکت می‌کند. ولت‌متر، پروب و ورودی‌های اسیلوسکوپ بار قابل توجهی به مدار اضافه نمی‌کنند.

## محدودیت مدل

این پروژه یک شبیه‌ساز آموزشی است و ابزارهای اندازه‌گیری نیز از همان مدل ساده‌شده مدار استفاده می‌کنند. آمپرمتر مقاومت داخلی بسیار کوچک دارد و اسیلوسکوپ فقط ولتاژ گره‌های حل‌شده را نمونه‌برداری می‌کند. نویز، پهنای باند، امپدانس ورودی واقعی تجهیزات و رفتارهای دقیق یک اسیلوسکوپ یا مولتی‌متر آزمایشگاهی مدل نشده‌اند.

## جمع‌بندی

بخش نهم لایه مشاهده و اندازه‌گیری را به شبیه‌سازی اضافه می‌کند، بدون اینکه نتیجه‌ای جدا از موتور اصلی تولید کند. کاربر می‌تواند ولتاژ یک نقطه را سریع بررسی کند، ولتاژ و جریان را با قطعات قابل سیم‌کشی ببیند و دو سیگنال را در طول زمان روی اسیلوسکوپ دنبال کند. رفتار ابزارها با Run، Pause، Step و Stop هماهنگ است و روی همان گره‌ها و جریان‌هایی کار می‌کند که بقیه مدار از آن‌ها استفاده می‌کند.
